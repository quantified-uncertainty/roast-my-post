# pnpm configuration for monorepo
auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=true

# Ensure lockfile is frozen in CI
frozen-lockfile=false

# Better output for CI
reporter=append-only