# Monorepo Environment Variables Template
# 
# This file shows the structure of environment variables used across the monorepo.
# For actual configuration:
# - Copy relevant variables to each app's .env.local file
# - See ENV_SETUP.md for detailed instructions
#
# IMPORTANT: This is just a template. Each app should have its own .env.local

# === Shared Infrastructure ===
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# === Web App Variables (apps/web/.env.local) ===
# See apps/web/.env.example for full list

# === MCP Server Variables (apps/mcp-server/.env.local) ===  
# See apps/mcp-server/.env.example for full list