{
  "name": "@roast/domain",
  "version": "0.1.0",
  "description": "Core business domain logic for RoastMyPost",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --watch",
    "test": "echo 'No tests in domain package' && exit 0",
    "test:watch": "echo 'No tests in domain package' && exit 0",
    "test:unit": "echo 'No tests in domain package' && exit 0",
    "test:integration": "echo 'No tests in domain package' && exit 0",
    "test:ci": "echo 'No tests in domain package' && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@roast/db": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.8",
    "typescript": "^5.5.2"
  },
  "keywords": [
    "domain",
    "business-logic",
    "services"
  ],
  "author": "RoastMyPost Team",
  "license": "MIT"
}