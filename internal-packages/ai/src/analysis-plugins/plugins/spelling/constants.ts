/**
 * Constants for the spelling analysis plugin
 */

// Error processing
export const MAX_ERRORS_PER_CHUNK = 20;
export const MIN_CONFIDENCE_THRESHOLD = 30;
export const HIGH_IMPORTANCE_THRESHOLD = 50;

// Text truncation for logging
export const LOG_TEXT_TRUNCATE_LENGTH = 30;

// Sample size for language convention detection
export const CONVENTION_SAMPLE_SIZE = 2000;

// Confidence thresholds for language convention
export const LOW_CONFIDENCE_THRESHOLD = 0.8;
export const LOW_CONSISTENCY_THRESHOLD = 0.8;